name: main.yml
on:
 push:
    branches: [ master ]
jobs:
    build:
       runs-on: ubuntu-latest
       steps:
       - uses: action/checkout@v2
       - name: Run-one line script
         run: echo Hello,world!

